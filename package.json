{
  "name": "claudit",
  "version": "3.0.0",
  "description": "Local optimization suite for Claude Code — persistent project knowledge base, CLI-only",
  "type": "module",
  "bin": {
    "claudit": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
